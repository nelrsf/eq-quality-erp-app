import { PageRendererDirective } from './page-renderer.directive';

describe('PageRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new PageRendererDirective();
    expect(directive).toBeTruthy();
  });
});
